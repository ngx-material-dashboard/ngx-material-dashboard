{{! render name of component as header }}
# API Reference for {{ name }}

`import {{ngModule.name}} from @ngx-material-dashboard/widgets;`

{{! render shortText if component has comment }}
{{#if comment }}
{{ comment.shortText }}
{{/if}}

{{! render table of properties if component has them }}
{{#if properties }}
{{> properties this }}
{{/if}}

{{#if methods }}
{{> methods this }}
{{/if}}
