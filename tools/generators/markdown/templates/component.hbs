{{! render name of component as header }}
# API Reference for {{ name }}

`import {{curlyBracket true}}{{ngModule.name}}{{curlyBracket false}} from '@ngx-material-dashboard/widgets';`

{{#if selector}}
Selector: &lt;{{selector}}&gt;
{{/if}}
<hr/>

{{! render table of properties if component has them }}
{{#if properties }}
{{> properties this }}
{{/if}}

{{#if methods }}
{{> methods this }}
{{/if}}
