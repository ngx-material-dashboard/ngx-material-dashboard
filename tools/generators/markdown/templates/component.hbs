{{! render name of component as header }}
### {{ name }}

{{#if decorator.selector}}
Selector: &lt;{{decorator.selector}}&gt;
{{/if}}
<hr/>

{{! render table of properties if component has them }}
{{#if properties }}
{{> properties this }}
{{/if}}

{{! render methods if component has them }}
{{#if methods }}
{{> methods this }}
{{/if}}