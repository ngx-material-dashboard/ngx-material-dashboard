{{! render name of class as header }}
## {{ name }}

{{! render shortText if class has comment }}
{{#if comment }}
{{ comment.shortText }}
{{/if}}

{{! render table of properties if class has them }}
{{#if properties }}
{{> properties properties }}
{{/if}}
