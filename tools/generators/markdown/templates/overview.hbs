{{! render name of class as header }}
## {{ name }}

{{#if comment }}
    {{#if comment.shortText}}
{{! render shortText if it exists }}
{{{ comment.shortText }}}
    {{/if}}

    {{#if comment.text}}
{{! render text if it exists }}
{{{ comment.text }}}
    {{/if}}
{{! 
    if no comment then render description if it exists this works for decorator
    comments
}}
{{else if description }}
{{{ description }}}
{{/if}}
