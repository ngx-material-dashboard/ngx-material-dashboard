import { NgModule } from '@angular/core';

/**
 * The `TestingModule` isn't needed for actual tests, it is just included to
 * keep things consistent when generating documentation for each project so we
 * don't need a special case just for the testing project.
 */
@NgModule({})
export class TestingModule {}
