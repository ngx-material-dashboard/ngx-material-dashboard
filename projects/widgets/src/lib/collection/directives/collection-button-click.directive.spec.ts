import { CollectionButtonClickDirective } from './collection-button-click.directive';

describe('CollectionButtonClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CollectionButtonClickDirective();
    expect(directive).toBeTruthy();
  });
});
