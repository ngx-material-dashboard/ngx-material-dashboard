/**
 * Const used to track metadata for attributes. Used to track things like new
 * and old values, serializedName, and whether attribute is dirty.
 */
export const AttributeMetadata = Symbol('AttributeMetadata') as any;
